import { toYesNo } from 'parking-web/helpers/to-yes-no';
import { module, test } from 'qunit';

module('Unit | Helper | to yes no');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toYesNo([42]);
  assert.ok(result);
});
